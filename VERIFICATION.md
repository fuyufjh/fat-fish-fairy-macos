# 验证记录

环境：macOS 26.6.2 / Apple Silicon，Swift 6.3.3（Command Line Tools）。2026-09-18。

## 默认内置萝莉小妹抖

- 仓库收录 `index.json`、`Character.md` 和 34 张动画帧；10 组动作齐全，未收录不用的 reference.png。
- SwiftPM 资源目录和独立 `.app` 资源目录均支持加载；新配置默认主题 ID 为 `loli_maid`。
- 97 项离线检查通过，包含默认主题、动作数量、性格文本及每张 PNG 解码验证。
- 重新构建 DMG，只读挂载后逐文件比较全部 36 个主题文件与源码一致，应用签名验证通过。
- Computer 中确认外观页第一项内置角色已选中，角色图片正常显示。当前用户主题已切换到内置版本，对话、记忆及其他设置保留。

## DMG 构建

- 根目录 `./build.sh` 成功生成 `dist/FatFishFairy.app` 和 `dist/FatFishFairy-1.0.0-arm64.dmg`。
- `hdiutil verify` 校验通过；只读挂载后确认应用、指向 `/Applications` 的快捷方式、安装说明及第三方声明齐全。
- 挂载镜像内的应用通过 `codesign --verify --deep --strict`；未包含 API 密钥、`.secret` 或本地状态文件。
- `bash -n` 通过，未知参数返回退出码 2。构建产物在被忽略的 `dist/` 中，不进入 Git。
- 默认 ad-hoc 签名；未进行 Apple 公证或跨架构安装验证。

## 多显示器回复格式修复

- 新建配置默认 `allDisplays = true`；已有用户选择保留，当前用户配置已开启。
- 回复缺少可选 activity / memories 时使用安全默认值；speech 缺失或类型错误仍拒绝，不把解析失败当成安静。
- 区分空内容、输出截断、JSON 语法和字段错误；格式错误只重试一次，2048 → 4096 输出 token，复用同一批截图。
- 多图提示明确要求合并为一个 JSON 对象；历史 assistant 消息也按相同 JSON 格式发送。
- `./scripts/test.sh`：60 项检查通过。使用隔离 URLSession 测试空回复、截断、数组、字段类型错误、一次重试恢复、耗尽重试、401 和内容过滤；校验重试未遗漏或更换图片。
- 真实 API：文字、单图识别及双图响应格式通过。纯色多图的颜色判别在探索测试中不稳定，因此双图测试验证传输及响应格式，不承诺模型识别准确性。
- 实机重新授权更新后的应用后，自动观察成功。`last-request.json` 记录 `imageCount: 3`、`finishReason: stop`、`outcome: success`，首轮成功（64 completion tokens），未触发格式重试。
- 原报错未保存响应正文，无法追溯当时究竟是截断、空内容还是字段不符；新增的诊断只记录结构元数据，不保存截图或回复正文。
- 保留用户的角色、对话、记忆、所有显示器及自动观察设置。

## 构建与自动化检查

- `./scripts/build.sh`：Release 构建成功，生成本机 ad-hoc 签名的 `.app` 与图标。
- `./scripts/test.sh`：16 项离线检查通过，覆盖密钥解析、JSON 回复、安静回复、本地恢复、文件权限、损坏数据和主题索引。
- `FATFISH_LIVE_TEST=1 ./scripts/test.sh`：真实 `deepseek-flash` 文字对话、JPEG 图片输入通过。合成红色图首次语义断言失败，复测通过；因此又通过桌面界面上传小鱼图标，确认模型正确识别蓝色、白色肚皮和粉色腮红。模型识别具有概率性，测试没有使用模拟回复。
- `codesign --verify --deep --strict dist/FatFishFairy.app`：通过。
- 检查应用包全部文件：未包含 `.secret` 文件或 API 密钥内容。

## Computer 实际操作

- 启动中文主窗口，检查聊天、外观、设置、记忆页面布局。
- 发送测试偏好「记住我喜欢喝拿铁」，真实模型回应并写入记忆。
- 重启应用，确认记忆和导入主题保留。
- 切换蜜桃/蓝色配色，检查界面和角色同步变化。
- 通过文件选择框导入上游 `loli_maid` 测试主题，确认逐帧角色图片显示。
- 通过文件选择框上传合成小鱼图标，模型正确识别内容。
- 关闭主窗口后桌面角色继续存在，双击角色重新打开主窗口。
- 用户在 macOS 中授权 `FatFishFairy.app` 屏幕录制后，手动观察成功产生「看了看屏幕」消息。
- 开启自动观察，自动产生后续屏幕回应；点击暂停后恢复「开启自动观察」状态。
- 测试结束保持自动观察关闭，测试对话/偏好从日常状态中移出，测试状态备份仅在 `.build/checks/`。

## 尚未实机覆盖

- Computer 的坐标拖动接口对不激活的悬浮面板返回 `noWindowsAvailable`，未能自动完成拖动测试。拖动由 AppKit `performDrag(with:)` 实现，位置保存与恢复有离线覆盖。
- 未主动锁定用户电脑、切换会话或拔插显示器；这些路径已实现事件监听、取消请求与可见区域恢复，但未做实机扰动测试。
- 未进行 Developer ID 签名、公证、跨机器安装或 Intel 构建验证。


## UI 模型配置（2026-09-18）

- 用设置表单替换 `.secret` / 环境变量读取；默认官方 DeepSeek Base URL、`deepseek-flash`、空 API Key、`thinking=disabled` 和 `reasoning_effort=none`。
- 密钥存入 macOS 钥匙串，按 Base URL 隔离；旧配置文件兼容迁移并保留原有聊天与偏好。保存失败不切换运行配置。
- 122 项离线检查通过，覆盖旧数据迁移、URL 校验、钥匙串新增/替换/删除及地址隔离、四档思考参数和自定义请求地址/模型/授权头。
- Computer Use 实测默认控件、无效 URL 拒绝保存、low 与 enabled 联动、保存和重启恢复；测试结束恢复 disabled/none，密钥保持为空。
- 真实 deepseek-flash 对话、单图识别和多图回复格式测试通过，仅发送测试文字及合成图片。多图合成颜色判断不完全准确，格式及传输通过不代表视觉准确率保证。
- Release App 编译、签名校验和 DMG 校验通过；安装包不包含密钥或开发机项目路径。

## 简化 API Key 配置（2026-09-18）

- 移除钥匙串读写与按地址保存逻辑，API Key 随连接配置写入本机 state.json，沿用 0600 文件权限。
- 使用绑定实际配置值的普通 SecureField；删除密钥存储说明及清除勾选项，清空输入框后保存即清除。
- 兼容旧版不含 apiKey 的连接配置，密钥默认为空，保留其地址、模型与思考强度。
- 121 项离线检查通过，包含 API Key 保存后恢复及清空；Release App 和 DMG 重新构建并校验通过。

## 单一形象、新图标与高级选项布局（2026-09-20）

- 将用户提供的 new_logo.png 原样收录为 Assets/AppIcon.png，打包时生成 16–1024 像素图标并写入 ICNS。
- 只保留原 loli_maid 动画，显示名改为「蓝色小肥鱼」；移除配色绘制形象、导入入口和主题切换菜单，旧选择在启动时统一归一，保留聊天记忆。
- 高级选项改为显式折叠按钮和左对齐内容容器，消除 macOS DisclosureGroup 内容居中产生的大段留白。
- 121 项离线检查通过；Computer Use 确认单一形象、正确名称与高级选项展开后的左对齐布局。Release App 签名及 DMG 校验通过。

## 提示词拆分、主题导入、调用日志与 SQLite（2026-09-20）

- 系统场景/输出协议提取到 SystemPrompt.md，Character.md 保留人设、语气与示例；请求中使用独立 system 消息。设置新增编辑框、保存及恢复默认，SQLite 保存覆盖值，下次请求生效。
- 恢复主题文件夹导入、已有导入形象加载、菜单及页面切换；内置仍仅蓝色小肥鱼，切换不删除聊天。
- app.log 以 JSON Lines 记录启动、每次模型 request / response、HTTP 状态、重试次数、关联 ID 与网络错误。完整请求含图片 Base64；Authorization 不入日志，当前 API Key 在正文中脱敏。日志 0600、50 MiB 轮换、最多 3 份备份。
- 使用系统 SQLite，无额外运行时依赖。设置、聊天、记忆事务保存；旧 JSON 自动迁移，原文件保留。保存部分历史页面不会删除未加载记录。
- 窗口打开只读取最新 50 条，使用 LazyVStack，顶部每次加载更早 50 条。后台观察保存后只保留最近 50 条在内存中，数据库保留完整历史。
- 141 项离线检查通过：迁移保真及失败恢复、等时间戳分页、重复保存去重、删除记忆不复活、提示词拆分与覆盖、完整请求及错误日志、密钥脱敏、日志轮换与权限。
- Computer Use 验证已有自定义形象切换、历史加载、系统提示词编辑/保存/重启恢复/恢复默认。实际数据库保留全部旧版消息，迁移过程中旧进程最后写入的 1 条也已补入；integrity_check 为 ok，两次启动日志均显示 loaded_messages=50。
- Release 构建、App 签名检查和 DMG 校验通过。本轮没有向真实 LLM 发送测试消息；网络请求覆盖使用 URLProtocol 模拟服务。

## 精简图片日志（2026-09-20）

- app.log 中图片 data URL 替换为 MIME 类型和编码长度占位信息，不再记录图片 Base64；嵌套数组及响应正文中的图片同样处理。
- 仅处理日志副本，HTTP 请求仍携带完整图片；API Key 脱敏继续生效。
- 143 项离线检查通过，新增实际发送图片不变、多图片及响应图片日志体积受控验证。Release App 与 DMG 构建校验通过。既有日志不回写。

## 统一界面与状态栏图标（2026-09-20）

- 将图标源文件移至 Resources/AppIcon.png，SwiftPM、App 包和 ICNS 构建共用同一资源。
- 替换侧边栏标志、聊天头像和菜单栏状态图标；状态栏使用 18×18 彩色非模板图像，避免系统将角色图标染成单色。
- Computer Use 截图确认侧边栏及聊天头像显示新图标；Release App 构建、签名检查与 DMG 校验通过。

## 夜间模式气泡对比度（2026-09-20）

- 桌面气泡及状态文字使用系统 primary 前景色，气泡边框同步适应外观，修复深色磨砂背景上固定深蓝文字难以辨认的问题。
- 使用实际 PetSpeechBubble 组件分别渲染深色和浅色预览，确认浅色文字/深色背景、深色文字/浅色背景均清晰。Release App 和 DMG 构建校验通过。

## 新增上游皮肤（2026-09-21）

- 内置主题加载全部打包资源，默认蓝色小肥鱼排在首位；新增「长大的妹抖」（36 帧）与「纳垢灵」（30 帧），包含各自 Character.md。
- 上游 grown_maid_long 仅有 reference.png，缺少主题配置及动画，未作为可用皮肤加入。
- 217 项离线检查通过，覆盖三套主题名称、默认顺序、人设及全部 100 张动画帧加载。
- Release App 构建、签名检查和 DMG 校验通过；核对 App 内三套主题的全部动画帧与源码资源逐字节一致。

## 移除纳垢灵与精简人设（2026-09-21）

- 删除 nurgling 资源及名称配置，内置只保留蓝色小肥鱼和长大的妹抖，同步更新文档、安装说明与主题检查。
- 两份 Character.md 分别精简为 64、63 字（含标点），保留身份、性格、称呼及语气。
- 185 项离线检查通过；Release App、签名及 DMG 校验通过。确认 App 无 nurgling 目录，打包人设与源码一致且均少于 100 字。

## 读屏摘要与发言分离（2026-09-21）

- 读屏响应新增必填非空 screenContent 工作摘要，与 speech 分离；兼容已有自定义系统提示词，缺失或空摘要按格式错误重试。
- 读屏请求仅使用当前截图及最近三次成功观察的摘要作为历史，不带旧 speech、手动聊天或长期记忆。手动对话历史过滤读屏发言，聊天窗口仍保留这些发言。
- SQLite 新增独立 screen_observations 表，只保留最近三次摘要；安静时也更新，重启后恢复，旧数据库自动建表且不从旧 speech 推断摘要。
- 207 项离线检查通过，覆盖摘要缺失重试、三次窗口、重启恢复、安静摘要、自定义提示词及手动聊天隔离。Release App 构建、签名检查与 DMG 校验通过；未调用真实 LLM。

## 固定本机代码签名（2026-09-21）

- 创建 FatFishFairy Local Code Signing 自签名身份，私钥导入登录钥匙串；仓库仅保存公开 DER 证书 signing/FatFishFairy.cer，临时私钥已清理。未修改系统证书信任或屏幕录制权限。
- 构建固定 Bundle ID 与证书 SHA-1 指纹的 designated requirement；证书私钥缺失或尝试覆盖为其他身份时失败，不回退 ad-hoc。
- Release App、签名校验和 DMG 校验通过。通过改变临时 App 副本的资源并重新签名，确认 CDHash 改变而 designated requirement 保持完全相同，且新版本通过旧身份规则校验。
- 验证 ad-hoc 覆盖与重复生成证书均被拒绝，Shell 语法与 diff 检查通过。屏幕录制权限跨真实安装保留尚未实测；首次从旧临时签名切换仍需用户重新授权。
